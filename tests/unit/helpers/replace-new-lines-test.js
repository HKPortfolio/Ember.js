import { replaceNewLines } from 'emberjs/helpers/replace-new-lines';
import { module, test } from 'qunit';

module('Unit | Helper | replace new lines');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = replaceNewLines([42]);
  assert.ok(result);
});
