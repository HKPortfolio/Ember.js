import { summarise } from 'emberjs/helpers/summarise';
import { module, test } from 'qunit';

module('Unit | Helper | summarise');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = summarise([42]);
  assert.ok(result);
});
